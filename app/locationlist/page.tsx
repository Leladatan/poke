import {NextPage} from "next";
import LocationsList from "@/app/locationlist/components/LocationsList";

const LocationListPage: NextPage = () => {
    return (
        <>
            <LocationsList />
        </>
    )
};

export default LocationListPage;
